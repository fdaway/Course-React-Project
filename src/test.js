const a = [1,5,2]
const b = [2,4,1]

var addTwoNumbers = function(a,b) {
    let sum = 0
    let g1 = 0
    let g2 = 0
    for(let i = l1.length-1; i>=0; i--){
        g1.push(l1(i))
    }
    console.log(g1)
    for(let i = l2.length-1; i>=0; i--){
        g2.push(l2(i))
    }
    console.log(g2)
    console.log(sum)
    const sum1 = g1.join('')
    const sum2 = g2.join('')
    sum = sum1 + sum2
    console.log(sum)
};
